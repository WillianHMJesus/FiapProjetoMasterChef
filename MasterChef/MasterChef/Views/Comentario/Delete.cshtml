@model MasterChef.Models.ComentarioModel

@{
    ViewData["Title"] = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Comentario</h4>
    <hr />
    @using (Html.BeginForm("Delete", "Comentario", FormMethod.Post))
    {
        <dl class="dl-horizontal">
            @Html.HiddenFor(model => model.Id)
            <dt>
                @Html.DisplayNameFor(model => model.Nome)
            </dt>
            <dd>
                @Html.DisplayFor(model => model.Nome)
            </dd>
            <dt>
                @Html.DisplayNameFor(model => model.ComentarioTexto)
            </dt>
            <dd>
                @Html.DisplayFor(model => model.ComentarioTexto)
            </dd>
        </dl>

        <input type="submit" value="Delete" class="btn btn-default" /> <div>|</div>
        @Html.ActionLink("Back to List", "Index", "Comentario");
    }
</div>
